package com.mesh.network.listeners.impl;

import com.mesh.network.listeners.InMessageListener;

public class InMessageListenerImpl implements InMessageListener {
	
	@Override
	public void messageReceived(String data) {
		// Convert to bean
		// Check its type
		// Send to executor
	}
}